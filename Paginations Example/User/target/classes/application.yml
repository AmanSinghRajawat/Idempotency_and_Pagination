spring:
  application:
    name: User

  datasource:
    url: jdbc:mysql://localhost:3306/PaginationAndIdempotency
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true



server:
  port : 8080

logging:
  level:
    org:
      springframework:
        security: DEBUG